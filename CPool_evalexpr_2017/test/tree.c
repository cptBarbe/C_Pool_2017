/*
** EPITECH PROJECT, 2017
** tree
** File description:
** binary tree
*/

#include "my.h"

int tree(struct node *left, struct node *right, struct node *prev))
{
	left->value;
	left->sign;
	prev->value;
	prev->sign;
	right->value;
	right->sign;
	return (0);
}

struct node *nextvalue(char sign, int value)
{
	struct node *left;
	struct node *right;
	struct node *prev;
}

int main()
{
	return (0);
}
